
# lists are default arrays in python, these are dynamic, like vectors and store any and mixed datatypes
# Python do have arrays, which we can import, but they are not as widely used
# we do import arrays, but numpy arrays

#lists are mutable

friends = ["Apple", "orange", 0.5, 4, False, "true"]

print(friends[0])

friends[0] = 5

print(friends)

print(friends[1:4]) #slicing

